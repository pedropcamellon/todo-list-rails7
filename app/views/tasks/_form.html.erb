<%= form_for([@project, @project.tasks.build]) do |f| %>
  <div class="mb-3">
  <%= f.text_field :content, placeholder: "New task", class:"form-control" %>
  </div>
  <%= f.submit "Add task", class:"btn btn-primary" %>
<% end %>